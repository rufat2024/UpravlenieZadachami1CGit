
Процедура ПередЗаписью(Отказ)
	Если НЕ ЭтоГруппа Тогда 
		ТэгиТекст = "";
		Для Каждого Стр Из Тэги Цикл 
			ТэгиТекст = ТэгиТекст+"#"+Стр.Тэг+" "; 
		КонецЦикла;	
		ТэгиТекст = СокрЛП(ТэгиТекст);
		ЕстьФайлы = ?(файлы.Количество()>0, Истина, Ложь);
	КонецЕсли;
КонецПроцедуры
