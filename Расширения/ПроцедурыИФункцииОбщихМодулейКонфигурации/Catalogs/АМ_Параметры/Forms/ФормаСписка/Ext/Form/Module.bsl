
&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	Если ИмяСобытия = "ПроцедурыИФункции_ПараметрыОбновлены" Тогда
		Элементы.Список.Обновить();
		ОбновитьОтображениеДанных();
	КонецЕсли;	
КонецПроцедуры
